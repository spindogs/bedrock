{% extends "layouts/master.twig" %}

{% block content %}

    <h1>Search</h1>

    {% if posts %}
    <ul>
        {% for post in posts %}
        	<li>{{ post.title }}</li>
        {% endfor %}
    </ul>	
    {% endif %}

{% endblock %}
